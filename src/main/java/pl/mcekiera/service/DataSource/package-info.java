/**
 * Provides a interface with concrete implementation for retrieving relevant business logic data, from diversified and
 * impermanent data sources. Parsing of every source could contain different steps and processing levels, therefore interface
 * contain one basic generic method.
 */

package pl.mcekiera.service.DataSource;